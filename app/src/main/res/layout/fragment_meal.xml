<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="in.dimigo.dimigoin.ui.main.fragment.meal.MealTime" />

        <variable
            name="vm"
            type="in.dimigo.dimigoin.ui.main.fragment.meal.MealViewModel" />

        <variable
            name="date"
            type="String" />
    </data>

    <ScrollView
        android:id="@+id/mealScrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:overScrollMode="never"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <include
                layout="@layout/layout_title"
                app:icon="@{@drawable/ic_calendar}"
                app:sub="@{date}"
                app:title="@{@string/meal}" />

            <include
                layout="@layout/layout_meal_section"
                app:meal="@{vm.todayMeal.breakfast}"
                app:mealTime="@{MealTime.BREAKFAST}"
                app:time="@{`오전 8시 10분`}" />

            <include
                layout="@layout/layout_meal_section"
                app:meal="@{vm.todayMeal.lunch}"
                app:mealTime="@{MealTime.LUNCH}"
                app:time="@{`오후 1시 27분`}" />

            <include
                layout="@layout/layout_meal_section"
                app:meal="@{vm.todayMeal.dinner}"
                app:mealTime="@{MealTime.DINNER}"
                app:time="@{`오후 7시 21분`}" />

        </LinearLayout>

    </ScrollView>

</layout>
